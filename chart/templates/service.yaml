apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
spec:
  type: ClusterIP

  selector:
    app: {{ .Values.appName }}

  ports:
    - name: {{ .Values.portName }}
      port: {{ .Values.port }}
      targetPort: {{ .Values.targetPort }}