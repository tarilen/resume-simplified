apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}
  namespace: monitoring
  labels:
    grafana_dashboard: "1"

data:
  resume-slo.json: |-
{{ .Files.Get "dashboards/resume-slo.json" | indent 4 }}